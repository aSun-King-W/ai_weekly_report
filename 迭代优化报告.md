# 迭代优化对比报告

## 概览

| 指标 | Iter 1 (基线) | Iter 2 (Tool/Prompt优化) | Iter 3 (Layer2 日志) | Iter 4 (LLM-as-Judge) | **Iter 5 (架构绕过)** |
|------|------|------|------|------|------|
| 测试用例数 | 14 | 14 | 14 | 14 | 14 |
| 通过数 | 3 | 3 | — | 9 (AI Judge) | **12 (AI Judge)** |
| 通过率 | 21.4% | 21.4% | — | 64.3% | **85.7%** |
| 关键词匹配率 | 47.5% | 47.5% | — | 27.1% | 45.4% |
| AI Judge 平均分 | — | — | — | 7.79/10 | **8.30/10** |

## 本轮变更 (Iter 5)

**Layer 3 第2轮优化：架构级绕过消除内容虚构**

核心思路：不让模型有机会处理/传递提交数据，在工具结果层捕获真实 commits 后直接程序化生成报告。

变更：
- `inferReportOptions(query)` — 从自然语言推断报告选项（风格/长度/指标）
- 拦截 `generate_report` 调用：只提取 options，不执行
- 捕获 `get_github_commits` 返回的真实 commits
- 短路循环，直接调用 `aiService.generateReport(realCommits, options)`
- 无真实数据（空仓库/404/错误）时回退到原有逻辑
- 提交列表中加入 SHA 标识符（如 `` `a1b2c3d` ``），报告 LLM 可追溯

## 失败模式分析

| 失败类型 | Iter 1 | Iter 2 | Iter 5 | 说明 |
|---------|--------|--------|--------|------|
| tool选择错误 | 3 | 3 | 0 | bypass 消除了模型编造数据路径 |
| 输出结构混乱 | 5 | 5 | 0 | generateReport 直接输出，格式稳定 |
| 内容偏离 query | 3 | 3 | 2 | 剩余问题：日期参数推断、超长仓库名 |
| API调用失败 | 0 | 0 | 0 | — |
| **虚构提交数据** | — | — | **0** | ✅ 核心问题已解决 |

## 失败用例详情 (Iter 5)

1. **用例 #2（日期范围）** — AI 3.0分：bypass 后 `inferReportOptions` 未提取 "2024-01-01→01-07" 日期参数，报告 LLM 生成的时间范围与 query 不匹配
2. **用例 #12（超长仓库名）** — AI 4.3分：`facebook/react-native-community/react-native-app-auth` 解析为 3 段导致 GitHub 404，模型未能友好处理

## 后续方向

1. **第3轮优化**：修复 inferReportOptions 对日期范围的推断，改进超长仓库名解析
2. **Layer 4**：单元/集成测试 + CI 配置

---

*报告由 Layer 3 Iteration Tracker 自动生成于 2026-05-03T08:16:37.896Z*
